# 導入プロンプト（開発中のセッションへ渡すもの）

テンプレートを既存プロジェクトへ適用し、要件駆動へ切り替えるための貼り付け文。

## 設計意図（なぜこの形か）

初版は「あなたの理解を提示してください」とだけ書いていた。結果は**浅かった**。
原因は2つ。

1. **一往復で終えられる書き方だった。** 深掘りは押し戻しから生まれるが、
   その役割を人間に依存していた
2. **浅い答えを弾く仕掛けが無かった。** 抽象的な要約は"それらしく"見えるので、
   読んだだけで終われてしまう

そこで、**浅い作業では作れない成果物**を必須にしてある。実データの件数、
ファイル:行番号、自分で作った境界事例、不明リスト。これらは調べないと書けない。

---

## 貼るもの（開発途中から入る場合）

```
このプロジェクトの進め方を、標準の開発テンプレートに合わせたい。

まず C:\Claude\dev-template の README.md を読んで運用モデルを把握し、
次を実行して全体ルールを適用してください。

    cd C:\Claude\dev-template; .\適用.ps1 -Global

適用後は ~/.claude/CLAUDE.md と skill: requirement-driven が有効になります。
skill: requirement-driven のフェーズ0（真の要件を見つける）に従ってください。

## 進め方

いきなり実装に入らないでください。また、**一往復で終わらせようとしないで
ください**。最低2往復は想定しています。1回目で私が差分を指摘するので、
それを反映して2回目を出してください。

### 1. 先に調べる（提示より前）

次を実際に読んでから書いてください。読んだ結果を先に報告すること。

- リポジトリ構造と主要モジュールの責務
- README / CLAUDE.md / 設計文書
- **git log**（過去の方針転換の痕跡。「戻した」「やめた」コミットは要件の化石）
- **実データ・設定・ログを実際に数える**（件数・分布・例外の割合）

報告には「何を読んだか」「数えた結果の数字」「読めなかったもの」を含めること。

### 2. 層を分けて提示する（**ここが一番大事**）

要件を平らに並べないでください。**軸・制約・要件は性質が違います**。

| 層 | 何か | 変わるか |
|---|---|---|
| 軸 | このアプリ全体で叶えたいこと。存在理由 | 変わらない。変わったら別のアプリ |
| 制約 | 環境・コスト・技術・運用上の制限 | 変わりうる |
| 要件・実装 | 軸を制約下で実現した形 | 制約が変われば変えてよい |

まず **軸を1〜2文で言い切ってください**（外すと以降が全部ずれます）。
次に制約を列挙し、最後に要件を次の表で出してください。

| # | 現状の要件・実装 | どの軸に効く | どの制約で歪んだか | その制約が消えたらどう変わるか | 根拠（ファイル:行 or 件数） |

**「その制約が消えたらどう変わるか」が最重要列です。** 変わらないなら軸に近く、
形が変わるなら制約由来の歪みなので聖域にしてはいけません。答えられない行が
あれば、制約が言語化されていないということなので、そこを私に聞いてください。

散文は反論しづらいので表にしてください。**私が「それは違う」と具体的に
指摘できる粒度**で書くこと。反論できない書き方（「利便性を高める」等）は
情報がゼロなので書かないでください。

### 3. 歪みを疑う

次の兆候がある要件は、制約由来の歪みが軸として固まっている可能性が高いので
必ず取り上げてください。

- **禁止・制約の形で書かれている**（「〜するな」「〜を使うな」）
- **本来の不変条件ではなく安価な代理指標で書かれている**
- **特定の実装名・製品名が要件に入っている**（手段が目的の席に座っている）

実例: 別プロジェクトで「LLM禁止」と言われていたが、軸は「人手をかけず正しく
収める」、制約は「継続課金の回避」で、禁止は歪んだ要件だった。軸も制約も
変わっていないのに要件だけが独り歩きし、課金の起きないローカルLLMという手段
まで失っていた。

### 4. 境界事例を自分で作って聞く

**あなたが作った境界事例を最低5つ**出してください。私が例を出すのを待たない
こと。yes/noか二択で答えられる形にしてください。ここが一番効きます。

### 5. 「ただし」を掘る

- 一括適用してよい範囲はどこまでか
- 人の確認が要るのはどこか
- 例外的に扱っている対象はあるか（なぜ例外なのか）

私が言語化していない前提はここに埋まっています。

### 6. 不明リストを出す

**「不明」と書く項目を最低3つ**挙げてください。推測で埋めないこと。
全部分かったという報告は、調べ方が浅いことを意味します。

## 禁止

- 一往復で完成させようとすること
- 実データを見ずに推測で要件を埋めること
- 重要度つきの修正リスト（A→B→C）を出して着手可否を聞くこと
- 応急処置の提案（私が明示的に頼むまで）

## 合意後

確定した要件をこのプロジェクトの CLAUDE.md に書き出してください。
CLAUDE.md が無ければ次で作れます。

    cd C:\Claude\dev-template; .\適用.ps1 -Project <このプロジェクトのパス>
```

---

## 貼るもの（新規に始める場合）

```
新しくこのプロジェクトを始めます。まず C:\Claude\dev-template の README.md を読み、
次を実行して全体ルールを適用してください。

    cd C:\Claude\dev-template; .\適用.ps1 -Global
    cd C:\Claude\dev-template; .\適用.ps1 -Project <このプロジェクトのパス>

そのうえで、**真の要件を定義できるまで実装に入らないでください**。私が語る要求は
真の要件の誤った具体化であることがあります。特に「〜するな」という制約の形で
伝えたものは、目的ではなく手段を言っている可能性が高いので疑ってください。

skill: requirement-driven のフェーズ0に従い、次を必ずやってください。

1. 「なぜそれが必要か」を1段上げて確認する
2. **例外と「ただし」を必ず聞く**（一括適用してよい範囲／人の確認が要る範囲）
3. 私に具体例を1つ出させ、**あなたが境界事例を最低5つ作って**突き合わせる
4. 事実で決められることは実データで確かめる
5. **「不明」と書く項目を最低3つ**挙げる。全部分かったという報告は浅い証拠

一往復で終わらせないでください。最低2往復を想定しています。

要件が固まったら CLAUDE.md の穴埋め箇所を実際の内容に置き換えてください。
```

---

## それでも浅いとき

次のどれかを返すと深くなる。

- **「軸と要件が混ざっている。このアプリ全体で叶えたいことを1〜2文で言い切って」**
- **「その要件は、どの制約で歪んだもの？ 制約が消えたらどう変わる？」**
- **「表の根拠列に数字が入っていない行がある。実データで数えて」**
- **「境界事例が私の言ったことの言い換えになっている。あなたが思いつく
  『これはどっち？』を出して」**
- **「不明が3つ未満。分かったつもりになっている箇所を探して」**
- **「それは私の発言の要約であって、要件ではない。目的は何？」**

## 補足

- **プロジェクト固有スキルを作るときは必ず `-Skill` を使う**。手書きすると
  聖典を覆い隠す事故が起きる
- 現場での改変は自由。**他プロジェクトでも通用すると分かったものだけ**を
  `global/` へ昇格させる
