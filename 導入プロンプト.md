# 導入プロンプト（開発中のセッションへ渡すもの）

テンプレートを既存プロジェクトへ適用し、要件駆動へ切り替えるための貼り付け文。
**開発途中から入る場合**を想定している（新規着手なら後半の「新規の場合」を使う）。

---

## 手順

1. 対象プロジェクトのセッションで、下の「貼るもの」をそのまま貼る
2. 相手が要件のすり合わせを求めてくるので、答える
3. 合意できたら実装に進ませる

---

## 貼るもの（開発途中から入る場合）

```
このプロジェクトの進め方を、標準の開発テンプレートに合わせたい。

まず C:\Claude\dev-template の README.md を読んで、運用モデル（聖典・配布物・現場）を
把握してから、次を実行して全体ルールを適用してください。

    cd C:\Claude\dev-template; .\適用.ps1 -Global

適用後は ~/.claude/CLAUDE.md と skill: requirement-driven が有効になります。
特に「要件の確定」と「問題対応の作法」に従ってください。

そのうえで、いきなり実装に入らないでください。このプロジェクトは開発途中なので、
**既存の実装・文書・データから当たりをつけて、あなたの理解を先に提示**してください。
「こう理解しているが合っているか」の形で出してくれれば、私が差分を指摘します。

提示してほしいのは次の3つです。

1. このプロジェクトが本当は何を達成しようとしているのか（あなたの理解）
2. 私が過去に出した指示のうち、**手段として語られているが目的は別にありそうなもの**
   （「〜するな」「〜を使え」の形をしたもの。実例: 別プロジェクトで「LLM禁止」と
   言われたが、真の要件は「継続課金の回避」でローカルLLMは問題なかった）
3. **要件として書かれていない暗黙の前提**で、コードや運用が依存しているもの

分からないことは「不明」と書いてください。推測で埋めないでください。
事実で決められることは実データで確かめてください。

合意できたら、このプロジェクトの CLAUDE.md に確定した要件を書き出してください。
CLAUDE.md が無ければ次で作れます。

    cd C:\Claude\dev-template; .\適用.ps1 -Project <このプロジェクトのパス>

なお以降、不具合や課題を見つけても、重要度つきの修正リスト（A→B→C）を出して
着手可否を聞くのはやめてください。要件・影響範囲を整理し「どうあるべきか」を
設計してから着手順を導出してください。応急処置は私が明示的に頼むまで提案しないで
ください。
```

---

## 貼るもの（新規に始める場合）

```
新しくこのプロジェクトを始めます。まず C:\Claude\dev-template の README.md を読み、
次を実行して全体ルールを適用してください。

    cd C:\Claude\dev-template; .\適用.ps1 -Global
    cd C:\Claude\dev-template; .\適用.ps1 -Project <このプロジェクトのパス>

そのうえで、**真の要件を定義できるまで実装に入らないでください**。私が語る要求は
真の要件の誤った具体化であることがあります。特に「〜するな」という制約の形で
伝えたものは、目的ではなく手段を言っている可能性が高いので疑ってください。

詰め方は skill: requirement-driven のフェーズ0に従ってください。特に、
例外と「ただし」を必ず聞くこと、私に具体例を1つ出させてからあなたが境界事例を
作って突き合わせること、をやってください。

要件が固まったら CLAUDE.md の穴埋め箇所を実際の内容に置き換えてください。
```

---

## 補足

- **プロジェクト固有スキルを作るときは必ず `-Skill` を使う**。手書きすると
  聖典を覆い隠す事故が起きる（README「同名スキルは聖典を覆い隠す」を参照）
- 現場での改変は自由。**他プロジェクトでも通用すると分かったものだけ**を
  `global/` へ昇格させる（README「昇格の手順」）
