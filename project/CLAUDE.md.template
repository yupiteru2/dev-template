# <プロジェクト名>

<1〜2行で何をするものか。言語・フレームワーク・実行環境>
仕様の正は <Document/○○.md>。
開発フロー・要件の確定・問題対応の作法は ~/.claude/CLAUDE.md（共通ルール）に従う。

## 不変条件（コードから読み取れない前提）

<「なぜそうなっているか」がコードを読んでも分からない約束事だけを書く。
 コードを読めば分かることは書かない（陳腐化するだけ）>

- 例: 破壊的操作の手順（copy→検証→原子的rename→元削除。直接削除は禁止）
- 例: 全操作をログに記録しundo可能に保つ。書き込みは必ず <Store> 経由
- 例: 外部I/Oが遅いので列挙は <専用関数> のみ。GUIの読み取りは <run_io>
- 例: 判定・変換は純関数モジュール、<gui/> は配線のみ
- 例: 実データに関わる変更は実環境で検証してから完了

## 問題対応の作法（プロジェクト固有があれば）

<共通ルールで足りるなら本節ごと削除してよい。
 過去に破綻した経緯があるなら、その教訓を具体的に書く>

## 検証コマンド

```powershell
<テスト・リンタ・型チェック・起動を、そのまま実行できる形で>
```

テスト補足: <時刻依存の排除方法・テスト対象外にしている層など>

## イシュー管理

<後回し課題をどこに置くか。依頼のされ方>

## 禁止事項

- <Webリモートへのpush可否>
- <手で触ってはいけないデータ>
